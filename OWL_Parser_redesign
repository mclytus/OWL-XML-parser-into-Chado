<?php
/*
 * @file 
 * // OWL redesign using Classes (Objects)
 */

// OWLStanza Class will be the parent Class which will parse the OWL file and that all the Classes(e.g Annotation Property, Object Property, Class, Description) would inherited. 

$owl = new XMLReader();
if (! $owl->open ($filename)) {
		//print "ERROR opening OWL file: '$filename'\n";
		exit ();
	}

$rdf = new OwlStanza();

Class OwlStanza {
	// private:
	// $owl = '';
	// $parse_children ='';
	// $tag_name = '';
	// $attributes = '';
	// $children = '';
	
	public function constructor(){
		
	}
	
	
	
}
